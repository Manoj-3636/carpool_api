services:
  - type: web
    name: locosync-api
    env: python
    buildCommand: ""
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=10000
    envVars:
      - key: MONGO_URI
        value: your-mongodb-uri-here